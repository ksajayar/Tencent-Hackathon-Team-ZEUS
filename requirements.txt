# --- M0: infrastructure ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
structlog==24.4.0
python-multipart==0.0.20

# --- M1: WhatsApp round trip ---
twilio==9.4.3

# --- M2: conversation with Gemini ---
google-genai==1.3.0
tzdata==2025.2

# --- M3: Google OAuth ---
google-auth-oauthlib==1.2.1
cryptography==44.0.0
apscheduler==3.11.0
psycopg2-binary==2.9.12  # sync driver, only for APScheduler's SQLAlchemyJobStore
requests==2.32.3  # direct call to Google's token-revoke endpoint

# --- M4: Calendar ---
google-api-python-client==2.154.0

# --- M5: Reminders ---
python-dateutil==2.9.0.post0

# --- M6: Voice ---
edge-tts==7.2.8
python-magic==0.4.27  # needs libmagic1 (already in the Dockerfile, M0)

# --- M8: Vision & documents ---
Pillow==11.1.0
pypdf==5.1.0

# Later milestones add: httpx, pytest/pytest-asyncio/respx/testcontainers.
# Do not add ahead of the milestone that needs them.
